<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= event.name %> - Event Details</title>
    <link rel="stylesheet" href="../src/styles/eventinfopage.css">
    
</head>
<body>
    <header>
        <h1><%= event.name %></h1>
    </header>
    
    <div class="event-details">
        <% if (event.image) { %>
            <img src="<%= event.image %>" alt="<%= event.name %> Image" class="event-image">
        <% } %>
        <p><strong>Description:</strong> <%= event.description %></p>
        <p><strong>Date:</strong> <%= new Date(event.date).toLocaleString() %></p>
        <p><strong>Location:</strong> <%= event.location %></p>
        <p><strong>Category:</strong> <%= event.category %></p>
        <p><strong>Hosted by:</strong> <%= event.hostname %></p>
    </div>
    
    <footer>
        <p>&copy; 2025 Event Platform. All rights reserved.</p>
    </footer>
</body>
</html>
